/**
 * Native (iOS/Android) counterpart to registerServiceWorker.web.js — intentionally
 * a no-op. Service workers / PWA install are web-only; the native apps have their
 * own offline support, so there is nothing to do here.
 */
export {};
